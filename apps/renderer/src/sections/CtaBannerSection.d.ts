interface Props {
    sectionId: string;
    props: Record<string, unknown>;
    isEditing?: boolean;
}
export default function CtaBannerSection({ sectionId, props, isEditing }: Props): import("react").JSX.Element;
export {};
